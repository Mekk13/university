1.
```
(defun f(l1 l2)
  (append (f (car l1 ) l2)
    (cond
      ((null l1) (cdr l2))
      (t (list (f (car l1) l2) (car l2)) )
    )
  )
)

(defun f(l1 l2)
  ((lambda (x)
    (append x
      (cond
        ((null l1) (cdr l2))
        (t (list x (car l2)))
      )
    )
  ) (f (car l1) l2) )
)
```

2.
```
f([],0).
f([H|T],S):-
    f(T,S1),
    S1 < H ,
    !,
    S is H.
f([_|T],S):-
    f(T,S1),
    S is S1.

g([],0).
g([H|T],S):-
    f(T,S1),
    S1 >= H,
    !,
    S is S1.
g([H|_],S):-
    S is H.
```

3.
```
(defun g(l)
  (mapcon #'LIST l)
)

(print (apply #'append (mapcon #'g '(1 2)) ))

rez: (1 2 2 2)
```

4.
```
f([],[]).
f([H|T],[H|S]):-
    f(T,S).
f([H|T],S):-
    H mod 2 =:= 0,
    f(T,S).
    
    answer: 1 2 3 , 1 3 on next. it is non-deterministic
```

5.
```
% i o o 
% select(l1..ln , C, R):-
% = (l1,l2..ln)
% = select(l2..ln, C, R) 

select([H|T],H,T).
select([_|T],C,R):-
    select(T,C,R).

cand(H,[H|_]).
cand(C,[_|T]):-
    cand(C,T).

len([],0).
len([_|T],N):-
    len(T,N1),
    N is N1 + 1.

fct(_,Acc,Acc,N,N):-!.
fct(L,[HAcc|TAcc],Rez,N,CN):-
    cand(C,L),
    C \== HAcc,
    \+ member(C,TAcc),	
    Y is HAcc - C , 
    Z is abs(Y),
    Z =< 3,
    CN1 is CN  + 1,
    fct(L,[C,HAcc|TAcc],Rez,N,CN1).

fct(L,Rez):-
    cand(C,L),
    len(L,N),
    fct(L,[C],Rez,N,1).
    
```

6.
```

```