1.
```
(defun f(l)
  (cond
    ((null l) nil)
    ((listp (car l)) (append (f (car l)) (f (cdr l)) (car (f (car l)) )))
    (t (list (car l)))
  )
)

;;only 1 rec call f (car l)

(defun g(l)
  (cond
    ((null l) nil)
    (t  ((lambda (x) 
      (cond
        ((listp (car l)) (append x (f (cdr l)) ) (car x) )
        (t (list (car l)))
      )
    )(f (car l)))  )
  )
)
```

2.
```
f(0,0):-!.
f(I,Y):-
    J is I-1,
    f(J,V),
    V>1,
    !,
    K is I-2,
    Y is K.
f(I,Y):-
    J is I-1,
    f(J,V),
    Y is V + 1.

% only one call of f(J,V):

aux(V,I,Y):-
    V>1,
    !,
    Y is I - 2.
aux(V,_,Y):-
    Y is V + 1.

g(0,0):-!.
g(I,Y):-
    J is I-1,
    f(J,V),
    aux(V,I,Y).
```

3.
```

(defun f(x &optional y)
  (cond
    ((null y) (cdr x))
    (t (cons (car x) y))
  )
)

(print (append (f '(1 2)) (f '(3 4) '(5 6)) ))
rez:
2 3 5 6 
```

4.
```
it will run forever and show 0 and all negative numbers following it 
```

5.
```
rory('R', Nry, Nry).
rory('Y', Nry, New) :- New is Nry + 1.

chairs(5,_,Acc,Acc).
chairs(Nrc,Nry,Acc,Rez):-
    rory(C,Nry,Nry1),
    Nry1 =< 3,
    Nrc < 5, % or ! at base case !!!
    Nrc1 is Nrc + 1,
    chairs(Nrc1,Nry1,[C|Acc],Rez).

chairs(Rez):-
    rory(C,0,Nry),
    chairs(1,Nry,[C],Rez).
    
    
```

6.
```
; sub(l, e, e1, crrk) =
; = [] , l - null
; = e1 , l - atom, l == e && crrk % 2 !=0 
; = l , l - atom && crrk % 2 ==0 
; = sub(l1,e,e1,crrk1) U sub(l2,e,e1,crrk1) U .. U sub(ln,e,e1,crrk1)  , l - list , l = l1..ln and crrk1 = crrk + 1 
; if li - list , or crrk otherwise.

(defun sub(l e e1 crrk)
  (cond
    ((null l) nil)
    ((atom l)
      (cond
        ((and (equal l e ) (equal 1 (mod crrk 2))) e1)
        (t l)
      )
    )
    (t (mapcar #'(lambda (x) 
        (cond
          ((listp x) (sub x e e1 (+ crrk 1) ) )
          (t (sub x e e1 crrk))
        )
    ) l ) )
  )
)

(print (sub '(1 d (2 d (d))) 'd 'f 1 ))
```