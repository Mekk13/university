
1.
```
(defun f(l)
  (cond
    ((null l ) 0)
    (t ((lambda (x)
        (cond
          ((> x 2) (+ (car l) (f (cdr l))))
          (t x)
        )
      ) (f (car l) )) 
    )
  )
)
```

2.
```
f([],0).
f([H|T],S):-
    f(T,S1),
    H<S1,
    !,
    S is H + S1.
f([_|T],S):- 
    f(T,S1),
    S is S1 + 2.

aux(S1,H,S):-
    H<S1,
    S is H + S1.
aux(S1,_,S):-
    S is S1 + 2.

f1([],0).
f1([H|T],S):-
    f(T,S1),
    aux(S1,H,S),
    !.
```

3.
```
(defun f(x &rest y)
  (cond
    ((null y) x)
    (t (append x (mapcar #'car y)))
  )
)

(print (append (f '(1 2)) (f '(3 4) '(5 6) '(7 8)) ))

rez: (1 2 3 4 5 7)
```

4.
```
p(1).
p(2).

q(1).
q(2).

r(1).
r(2).

s:-!,
    p(X),q(Y),r(Z),
    write(X),write(Y),write(Z),
    nl.
    
    
rez: 
111  
true
112  
true
121  
true
122  
true
211  
true
212 
true
221  
true
222
```

5.
```

%getElem(l1..ln)=
% = l1 , getElem(l2..ln)
% flow: (i, o ) 
getElem([E|_],E).
getElem([_|T],E):-
    getElem(T,E).


% gen(l1..ln,len) =
% = [] , n == 0
% = (E,R) = getElem(l1..ln), E U gen(R,len+1), E < last element added and len % 2 == 0 
%

gen(_,Len,Acc,Acc):-
    Len mod 2 =\= 0.
gen(L,Len,[HAcc|TAcc],Rez):-
    getElem(L,E),
    E < HAcc, 
    Len1 is Len + 1,
    gen(L,Len1,[E,HAcc|TAcc],Rez).
gen(L,Rez):-
    getElem(L,E),
    gen(L,1,[E],Rez).
```

6.
```

; f(l k crrk)=
; = [] , l -empty 
; = l , l - atom && crrk != k 
; = 0  , l - atom && crrk == k 
; = f(l1 k crrk1) U f(l2 k crrk1) U.. Uf(ln k crrk1) , crrk1 = k if li - atom , crrk1= k+1 otherwise

(defun f(l k crrk)
  (cond
    ((null  l) nil)
    ((atom l)
      (cond
        ((equal crrk k) 0)
        (t l)
      )
    )
    (t 
      (mapcar #'(lambda (x)
          (cond
            ((atom x) (f x k crrk) )
            (t (f x k (+ crrk 1)) )
          )
      ) l  )
    )
  )
)

(print (f '(a (1 (2 b)) (c(d))) '1 '1))
```
