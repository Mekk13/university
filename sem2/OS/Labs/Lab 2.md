sudo rm -rf myprogram to delete something (anything!)

compile: gcc <file.c> -o <file>  and then we execute
./name

w-> save q-> exit
esc :

a pointer to a list of pointers to arrays of characters 

<when we have args we print from 1 bc the exec arg is also an arg>

